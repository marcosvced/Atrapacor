<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="devlrmve.atrapacor.com.atrapacor">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/SpashScreen.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.Login_Activity"
            android:label="@string/app_name" />
        <activity
            android:name=".Activities.Main_Play"
            android:label="@string/app_name"
            android:theme="@style/Transparent.NoActionBar" />
        <activity
            android:name=".Activities.Main_Game"
            android:label="@string/app_name" />
        <activity
            android:name=".Activities.Main_Scores"
            android:label="@string/title_activity_main_scores"
            android:theme="@style/Transparent.NoActionBar" />
        <activity
            android:name=".Activities.Main_All_Scores"
            android:label="@string/title_activity_main_all_scores" />
        <activity
            android:name=".Activities.Main_Records"
            android:label="@string/title_activity_main_records" />
        <activity android:name=".Activities.Main_Tools" />
        <activity
            android:name=".Activities.SecondLayoutIntro"
            android:theme="@style/SpashScreen.NoActionBar" />
        
    </application>

</manifest>